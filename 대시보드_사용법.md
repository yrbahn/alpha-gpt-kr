# 🥧 Alpha-GPT-KR 대시보드 사용법

## 빠른 시작

```bash
cd /Users/yrbahn/.openclaw/workspace/alpha-gpt-kr
./start_dashboard.sh
```

접속:
- **같은 컴퓨터:** http://localhost:8888/dashboard.html
- **다른 컴퓨터:** http://YOUR_IP:8888/dashboard.html (스크립트가 IP 출력해줌)

## 자동 업데이트 기능

### 1. 서버 측 업데이트 (5분마다)
- 백그라운드에서 자동으로 `generate_dashboard.py` 실행
- DB에서 최신 데이터 읽어서 `dashboard.html` 재생성

### 2. 브라우저 자동 새로고침 (5분마다)
- HTML에 자동 새로고침 기능 내장
- 브라우저가 자동으로 최신 대시보드 표시

## 서버 중지

`Ctrl + C` 누르기

## 백그라운드 실행 (선택사항)

### nohup 사용
```bash
nohup ./start_dashboard.sh > dashboard_server.log 2>&1 &
echo $! > dashboard_server.pid
```

### 중지
```bash
kill $(cat dashboard_server.pid)
rm dashboard_server.pid
```

### 로그 확인
```bash
tail -f dashboard_server.log
```

## macOS 시작 시 자동 실행 (선택사항)

### launchd 사용

파일 생성: `~/Library/LaunchAgents/com.alphagpt.dashboard.plist`

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.alphagpt.dashboard</string>
    
    <key>ProgramArguments</key>
    <array>
        <string>/Users/yrbahn/.openclaw/workspace/alpha-gpt-kr/start_dashboard.sh</string>
    </array>
    
    <key>WorkingDirectory</key>
    <string>/Users/yrbahn/.openclaw/workspace/alpha-gpt-kr</string>
    
    <key>RunAtLoad</key>
    <true/>
    
    <key>KeepAlive</key>
    <true/>
    
    <key>StandardOutPath</key>
    <string>/Users/yrbahn/.openclaw/workspace/alpha-gpt-kr/logs/dashboard.log</string>
    
    <key>StandardErrorPath</key>
    <string>/Users/yrbahn/.openclaw/workspace/alpha-gpt-kr/logs/dashboard.error.log</string>
</dict>
</plist>
```

### 등록 및 시작

```bash
# logs 디렉터리 생성
mkdir -p /Users/yrbahn/.openclaw/workspace/alpha-gpt-kr/logs

# 서비스 등록
launchctl load ~/Library/LaunchAgents/com.alphagpt.dashboard.plist

# 시작
launchctl start com.alphagpt.dashboard

# 상태 확인
launchctl list | grep alphagpt
```

### 중지 및 제거

```bash
# 중지
launchctl stop com.alphagpt.dashboard

# 등록 해제
launchctl unload ~/Library/LaunchAgents/com.alphagpt.dashboard.plist

# 파일 삭제
rm ~/Library/LaunchAgents/com.alphagpt.dashboard.plist
```

## 포트 변경

`start_dashboard.sh`에서 8888을 원하는 포트로 변경:

```bash
python3 -m http.server 9999 --bind 0.0.0.0
```

## 외부 접근 방화벽 설정

### macOS 방화벽 허용

시스템 설정 > 네트워크 > 방화벽에서:
- Python 허용

또는 명령어:
```bash
sudo /usr/libexec/ApplicationFirewall/socketfilterfw --add /usr/local/bin/python3
sudo /usr/libexec/ApplicationFirewall/socketfilterfw --unblockapp /usr/local/bin/python3
```

### 공유기 포트 포워딩 (외부 인터넷에서 접근)

공유기 설정에서:
- 외부 포트: 8888
- 내부 IP: Mac의 IP (예: 192.168.0.123)
- 내부 포트: 8888
- 프로토콜: TCP

⚠️ **보안 주의:** 외부에 노출할 경우 인증 기능 추가 권장

## 문제 해결

### "Address already in use"

다른 프로그램이 8888 포트 사용 중:

```bash
# 포트 사용 확인
lsof -i :8888

# 프로세스 종료
kill -9 <PID>
```

### 대시보드가 업데이트 안 됨

```bash
# 수동 업데이트
python3 generate_dashboard.py

# 권한 확인
ls -la dashboard.html

# DB 연결 확인
python3 test_postgres.py
```

### IP 주소를 모름

```bash
# Mac IP 확인
ifconfig | grep "inet " | grep -v 127.0.0.1
```

## 대시보드 커스터마이징

`generate_dashboard.py`의 HTML 템플릿 부분 수정:
- 차트 색상
- 새로고침 주기 (`meta refresh` content 값)
- 표시할 데이터 개수

예: 1분마다 새로고침
```html
<meta http-equiv="refresh" content="60">
```

---

**빠른 시작:** `./start_dashboard.sh` 실행하고 브라우저에서 http://localhost:8888/dashboard.html 열기!
